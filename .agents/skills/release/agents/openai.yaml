interface:
  display_name: "Release"
  short_description: "Manual release branch and npm publish flow"
  default_prompt: "Use $release to prepare a manual npm release from the release branch."
policy:
  allow_implicit_invocation: false
